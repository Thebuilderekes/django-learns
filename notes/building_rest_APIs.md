 # REST API

REST which stands for representational state transfer is  a way to build APIs
that need no storage of information, just straight up communication between
database and client through the server over HTTP in a request and response
cycle.
